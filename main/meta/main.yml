---

galaxy_info:
  namespace: "nickjj"
  role_name: "docker"
  author: "Maksim Pikalov"
  description: "Install and configure Docker / Docker Compose."
  min_ansible_version: "2.10.0"

  platforms:
    - name: "Ubuntu"
      versions:
        - "focal"
        - "jammy"
        - xenial
        - yakkety
    - name: "Debian"
      versions:
        - "bullseye"
        - "bookworm"

  galaxy_tags:
    - "containers"
    - "compose"
    - "docker"
    - "packaging"
    - "system"
    - docker
    - docker-compose
    - ci
    - cd
    - deployment
    - production

dependencies:
  - role: angstwad.docker_ubuntu
